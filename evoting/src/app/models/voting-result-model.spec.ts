import { VotingResultModel } from './voting-result-model';

describe('VotingResultModel', () => {
  it('should create an instance', () => {
    expect(new VotingResultModel()).toBeTruthy();
  });
});
