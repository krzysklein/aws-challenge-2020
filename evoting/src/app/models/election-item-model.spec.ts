import { ElectionItemModel } from './election-item-model';

describe('ElectionItemModel', () => {
  it('should create an instance', () => {
    expect(new ElectionItemModel()).toBeTruthy();
  });
});
